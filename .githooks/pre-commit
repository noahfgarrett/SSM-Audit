#!/bin/sh
# Blocks a commit whose staged text contains a term from .confidential-terms.
node "$(git rev-parse --show-toplevel)/build/confidential-check.mjs" --staged || { echo "pre-commit: a staged file contains a confidential term (see above)." >&2; exit 1; }
